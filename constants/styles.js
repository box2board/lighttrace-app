// styles.js
// Shared style values that can be reused in screens and components.

const styles = {
  screenPadding: 16,
  screenPaddingLg: 28,
  borderRadius: 10,
  borderRadiusLg: 18,
};

export default styles;
